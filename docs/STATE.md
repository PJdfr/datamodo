# datamodo — current state (features · code map · stack · env)

> **Maintained doc — update with EVERY commit that changes behavior.**
> The rule lives in `CLAUDE.md`. Sibling docs: [FLOW.md](FLOW.md) (pipeline
> infographic), [ROADMAP.md](ROADMAP.md) (what's next), [MEMORY.md](MEMORY.md)
> (durable context). Dated history stays in [../PROJECT_STATE.md](../PROJECT_STATE.md).
>
> Last updated: 2026-07-17

## Stack at a glance

| Layer | Choice | Where |
|---|---|---|
| App | Next.js 16 (modified — read `node_modules/next/dist/docs/` first), React, TypeScript | `app/`, `components/` |
| DB | Neon Postgres 18 (pgvector, pg_trgm), branches `prod`/`dev`/previews | schema: `neon/schema.sql`, migrations: `neon/migrations/` |
| ORM | Prisma 7 + `@prisma/adapter-neon` | `prisma/schema.prisma`, `lib/prisma.ts` |
| Auth | Neon Auth (Better Auth), app-layer authz (`org_id` scoping, no RLS) | `lib/auth/*`, `app/api/auth/[...path]`, `proxy.ts` |
| Blobs | S3-generic adapter → Cloudflare R2 bucket `ingest` (eu) | `lib/storage/blob.ts` |
| LLM | Provider-agnostic interface: OpenRouter (default) / OpenAI / Anthropic / **Ollama (keyless, 2026-07-14)**; BYOK per user (for Ollama the BYOK field holds the user's SERVER URL). **Anthropic newest-gen fixes (2026-07-16)**: Sonnet 5 / Opus 4.7+ (incl. our escalate default `claude-sonnet-5`) reject `temperature` with a 400 AND run adaptive thinking when `thinking` is omitted (reply leads with thinking blocks; thinking bills against `max_tokens`) — `buildAnthropicBody` includes `temperature` only when `anthropicAcceptsSampling(model)` allowlists the family (claude-2/3.x, haiku-*, opus/sonnet 4.0–4.6) and sends `thinking:{type:"disabled"}` on the adaptive-default models (never on always-thinking families — `anthropicAlwaysThinks`); replies parsed via `extractAnthropicText` (scans for `text` blocks, not `content[0]`). Same gate on the OpenAI side: reasoning families (`o\d`/`gpt-5*` on the REAL OpenAI API only) get `max_completion_tokens` and no `temperature` (`isOpenAiReasoningModel`; OpenRouter/Ollama keep the classic shape). All pure + unit-tested | `lib/llm/*`, `lib/datamodo/llm-for-user.ts`, `anthropicAcceptsSampling`/`anthropicAlwaysThinks`/`buildAnthropicBody`/`extractAnthropicText` in `lib/llm/anthropic.ts`, `isOpenAiReasoningModel` in `lib/llm/openai-compatible.ts` (+`tests/llm-provider.test.ts`) |
| Email inbound | Cloudflare Email Worker → `POST /api/ingest` | `workers/email-ingest/` |
| Jobs | GitHub Actions cron → `extract-tick` (+ `after()` self-kick on ingest); daily `consolidate` (graph homeostasis) | `.github/workflows/extract-cron.yml`, `.github/workflows/consolidate-cron.yml`, `app/api/jobs/*` |
| Hosting | Vercel (Production=prod branch, Preview=dev branch) | — |
| Tests | `node:test` over pure cores (`npm test`) | `tests/*.test.ts` |
| CI | GitHub Actions: tests, tsc, lint==baseline, build — on every PR, dev/prod push, AND `claude/**` pushes (agent-session pushes don't fire `pull_request` events) | `.github/workflows/ci.yml`, `scripts/check-lint-baseline.mjs` |
| Screenshot harness | `npm run shoot [-- explorer schema timeline review]` — bundles a fixture harness (esbuild), renders it in local Chromium (playwright-core), writes `.shoot/<name>.png`, fails on page errors. No DB/login needed | `scripts/shoot/run.mjs`, `scripts/shoot/harnesses/*.tsx` |
| Design | datamodo design system (cream/ink/coral, Geist Mono data) | `design/system/`, skill `.claude/skills/datamodo-design/` |

## Feature inventory (shipped)

### Capture (channels in)
| Feature | Code |
|---|---|
| Email inbound (forward to `<token>@domain`), live E2E verified | `workers/email-ingest/`, `app/api/ingest/route.ts`, `lib/ingest/store.ts`, `lib/datamodo/inbox.ts` |
| WhatsApp inbound (Twilio, shared bot + link-code identify) | `app/api/webhooks/whatsapp/route.ts`, `lib/datamodo/channels.ts` |
| Slack inbound (signing-secret verify) | `app/api/webhooks/slack/route.ts` |
| Teams inbound (Bot Framework JWT verify) | `app/api/webhooks/teams/route.ts` |
| **IMAP pull — LOCAL edition BYOB capture (2026-07-15)**: a self-hosted install has no public URL for webhooks, so it PULLS from the user's own mailbox instead. Add a mailbox from **Settings → Mailboxes** in the dashboard OR `datamodo connect --host … --user … --pass …` — both write the same `0600` `connectors.json`. `datamodo serve` runs an in-process poller (imapflow) that, once the dashboard is up, **re-reads `connectors.json` every 60 s tick** (so dashboard add/remove takes effect within a minute, no restart) and watches each mailbox — first sight records the UID high-water mark (no backfill), then each NEW message's raw RFC822 is downloaded and POSTed to a local-only route which parses it (mailparser) and feeds it through the SAME capture pipeline as every other channel. Per-connector UID cursor persisted to `connectors-state.json`; message-id (namespaced by connector) is the idempotency key; on ingest rejection the cursor doesn't advance (retried next tick). Passwords are write-only (sent on add, never echoed back). Mapping core + runtime core (config/cursor/poll/store) both unit-tested with a fake IMAP client incl. hot-reload (no network). Live IMAP not exercised in CI | pure map `lib/local/connectors/imap.ts`, runtime `lib/local/connectors/imap-poll.mjs` (config/cursor/poll + shared store), ingest route `app/api/local/imap/route.ts`, management route `app/api/local/connectors/route.ts` (GET/POST/DELETE, local-only), `ConnectorsCard` in `control-center.tsx`, `localDataDir()` in `lib/local/config.ts`, shared kick `lib/ingest/kick.ts`, CLI `connect` + serve wiring in `bin/datamodo.mjs`; deps `imapflow`, `mailparser`; test `tests/imap-connector.test.ts` |
| Connect-a-channel UI (inbox address + link codes) | `app/dashboard/connections.tsx`, `createChannelLinkCodeAction` in `app/dashboard/actions.ts` |
| **In-app Chat channel (2026-07-12, brand pass same day)**: the app itself is a capture channel, peer to email/WhatsApp — a Chat tab with a thread + composer: auto-growing text, any-file attach + drag-drop + paste-to-attach (photos/PDF/docs/sheets, ≤8 files, 15 MB/message), dictaphone voice notes with in-tray audio preview + recording timer (MediaRecorder → the audio/transcription tier), live speech-to-text (Web Speech API, feature-detected); optimistic bubbles with image thumbnails, day separators, live pipeline status per message (pulsing "reading…" → "✓ filed", 4s poll while busy). Design per `design/system`: paper bubbles on cream, ONE coral accent, all data in Geist Mono, glyphs not emoji (⊕ ⏺ ∿ ▤ ▣), dm-drop-in/cc-pulse motion (reduced-motion safe). Evaluated chatscope/reachat/prompt-kit chat kits — rejected (own themes/Tailwind fight the brand); hand-rolled on the app's own motion vocabulary. Messages land as `upload`-channel items (`meta.via="app"`) with the same post-response extraction kick as /api/ingest. **Addressable (2026-07-14)**: a "to" chip row + inline `@agent` autocomplete (↑↓/Enter/Esc; purpose one-liners shown inline; sticky recipient; "→ agent" chip on sent bubbles) picks WHICH agent reads the message — the item stores `meta.agent_id`/`agent_name` and extraction steers on that agent's `purpose_text`; no addressee = the general datamodo agent, deterministic (pure mention core `lib/datamodo/chat-address.ts`; suggested-reroute is a later, LLM-gated step). **The thread is TWO-WAY**: pending reviews render as datamodo's own ink bubble ("✦ needs your OK" — numbered questions with ✓ yes / ✗ no buttons); tapping applies the review's real side-effects (same core as Review Studio and the WhatsApp reply) with an optimistic receipt line. **Full-width + discoverable drop (2026-07-13)**: the thread/composer fill the whole main column (was a 720px left column; bubbles capped at `min(78/84%, 640/700px)` to stay readable), and a persistent dashed drop affordance sits above the composer ("Drag & drop any doc here — or click to attach…") so attach/drag/paste is obvious even after the empty state clears **Alive + answering (2026-07-16)**: a three-dot TYPING bubble (ink, `dm-typing`) while the pipeline reads, and — the app thread being a DIRECT PING — every analyzed message gets the agent's ANSWER: an ink `datamodo` bubble listing what was parsed (entities with up to 3 inline facts, docs read, note kept, concept tags; staggered `dm-line-in` reveal) or "Nothing to file from this one". Same reply goes out over Slack DM/WhatsApp/direct email (`capture_mode='active'` only — IMAP-watched mailboxes stay silent, the bot never narrates an inbox); pure `buildParseReply`/`shouldSendParseReply` (+7 tests), app delivery via item `meta.parse_reply` → chat GET `reply`. Verified 5/5 E2E on the packed artifact | `app/api/chat/route.ts` (+ `questions` from `review-inbox`), `app/api/chat/review/route.ts`, `app/dashboard/chat-view.tsx` (`ReviewBubble`, `TypingBubble`, `AgentReplyBubble`), `lib/datamodo/parse-reply.ts` (+`tests/parse-reply.test.ts`), reply hook in `extract.ts`, `dm-typing`/`dm-line-in` in `globals.css`, Chat tab in `control-center.tsx`, harness `scripts/shoot/harnesses/chat.tsx` |
| Raw store: sha256+gzip deduped blobs, ref-counted; items + attachments | `lib/ingest/store.ts` (tables `items`, `blobs`, `attachments`) |

### Extraction pipeline
| Feature | Code |
|---|---|
| Queue: atomic claim (`FOR UPDATE SKIP LOCKED`), orphan recovery, retry cap, drain loop | `claimStoredItems`/`recoverExtractionQueue` in `lib/datamodo/extract.ts`, `app/api/jobs/extract-tick/route.ts` |
| Message extraction (two-model, confidence-gated escalation, onboarding-context steered) | `extractFromMessage`/`runExtractionForItem` in `lib/datamodo/extract.ts` |
| **Obsidian vault import, phase 1 (2026-07-16, user ask)** — deterministic ZERO-LLM migration: `.md` note → `note` entity with `body_md` = the note (wikilinks render live), `[[wikilink]]` → `mentions` edge (unresolved targets = note stubs; identity = basename so stubs and real notes converge), frontmatter `key: value` → typed facts (number/ISO-date/text; `[[X]]` values → edges with the key as predicate; lists = cardinality many), `#tags` + frontmatter tags → concept entities via `about`, `aliases:` → `also_known_as` facts. `POST /api/import/obsidian` (session-authed, ≤120 files/request — the client batches): default DRY RUN returns stats + folder shapes (≥3 notes sharing ≥2 frontmatter keys = a template announcing itself — preview only, category filing = phase 2); `confirm:true` ingests with `adjudicate:false` (new ingest option: deterministic resolution tiers only — a 3,000-note vault fans out zero LLM calls; the consolidation tick embeds + proposes merges later). Idempotent by content hash: one `items` row per note (`external_id "obsidian:<path>"`), unchanged no-op, edited supersede. **Phase 1b same day**: "⇪ Obsidian vault → knowledge" in the Data tab's Build menu opens `ObsidianImportModal` — directory picker (`webkitdirectory`), .md files read IN THE BROWSER, dry-run preview (counts + "folders that look like categories"), confirm imports in batches of 100 with running progress + done counts; and confirm now FILES the detected folder shapes as `category_proposal` reviews (deterministic template: fields typed from the notes' actual values, wikilink-valued keys become relations; kind = singularized folder slug; any-status dedupe). STILL open: `datamodo import obsidian` CLI, attachments | pure `lib/datamodo/obsidian-import.ts` (+`tests/obsidian-import.test.ts`), route `app/api/import/obsidian`, `adjudicate` option in `knowledge.ts` |
| **Per-row review graph preview (2026-07-16, user ask: "show what happens if we accept or refuse — for one row")** — every graph-shaped review row (entity_merge / orphan_prune / fact_conflict) gains a "◍" pill that opens a modal with the mini graph scene of THAT decision's two futures, toggled "✓ if you accept" / "✕ if you refuse": merges show two stars vs one node absorbing the loser's re-pointed edges (loser ghosted dashed-coral, shared neighbors collapse to one, gained edges coral); orphans show the strays kept vs fading dashed-red; conflicts show the subject with the disputed value solid vs struck. Deterministic layout (fixed positions, no physics — the Explorer's standing rule), each future annotated with one plain sentence (incl. the safety semantics: "only the ones STILL unlinked at accept time"). Works label-only on simulated rows (ids optional); view-models gained `sourceEntityId`/`targetEntityId`/`subjectEntityId`/orphan ids | pure `lib/datamodo/review-preview.ts` (+`tests/review-preview.test.ts`), modal `app/dashboard/review-graph-modal.tsx`, ◍ wiring in `review-studio.tsx` DiffRow, ids in `reviews.ts`/`review-types.ts` |
| **Unbounded documents + chunk-importance selection (2026-07-16, user call: "no PDF size limit, but classify which chunks matter")** — read caps became SAFETY CEILINGS (`MAX_DOC_PAGES` 20→500, `MAX_DOC_CHARS` 20k→600k, `MAX_CHUNKS` 60→500): the whole document is read, chunked, embedded, and searchable. What the distill LLM READS is decided by a pure zero-LLM scorer (`chunk-select.ts`): above the 24k-char prompt budget, classify from the head, then score every chunk (results/summary headings up, references/appendix down; digit/%/currency density; overlap with the classified kind's template vocabulary + agent purposes; opening chunks always kept) and fill the budget — output re-ordered by position with `[p.N]` markers and elision marks. Applied to the text AND audio-transcript branches (`distillInput` in documents.ts). A 200-page paper now costs the same prompt as a 20-pager while its appendix stays citable. **SEMANTIC LEG (2026-07-17, GRAPH_PIPELINE.md "Adaptive classifiers (2)")**: lexical `includes()` misses paraphrase ("risk-adjusted performance of 1.31" never contains the key `sharpe`) — so long documents now embed their chunks BEFORE selection (the same vectors are handed to `storeDocChunks`, which skips its own embed call — reordering made the leg free) and embed the org's CONTEXT ANCHORS (business context · agent purposes · kind templates, classified kind first; process-cached per org shape + embedding space); each chunk adds `semanticBoost(max cosine(chunk, anchor))` (floor .25, cap 3 — same ceiling as the lexical key-term sweep) to its structural score. Headings/density/position/key-terms all stay; no embeddings key → purely structural, exactly as before | pure `lib/datamodo/chunk-select.ts` (`cosineSim`/`semanticBoost`/`buildContextAnchors`, `chunkVectors`+`anchorVectors` in `selectChunksForPrompt`; +`tests/chunk-select.test.ts`), embed-before-select + anchor cache in `distillInput` (`documents.ts`), vector reuse in `storeDocChunks` (`chunks.ts`), ceilings in `document-extraction.ts` |
| **Efficiency track, step 1 (2026-07-16, user ask — GRAPH_PIPELINE.md "Efficiency track")** — ① triviality gate: unmistakable acks (en/fr lexicon, emoji-only, ≤80 chars, no digit/currency/URL/@/? signal) skip steering+priming+LLM entirely and file as analyzed (`model:"gate:trivial"`; chat still replies "Nothing to file"; attachments/note-subjects always pass); ② stable-prefix prompts: the categories block moved into the message SYSTEM prompt (per-org constant prefix) and the Anthropic provider sends systems ≥4000 chars as `cache_control` blocks (~90% cached-input discount; OpenAI prefix-caches automatically; mock accepts both shapes); ③ adjudication floor/cap (candidates ≥0.25 sim, top 3 — no candidate = new entity at zero cost); ④ search fast path (`listKnowledge({provenance:false})` skips fact_sources+items joins on search/GraphRAG); ⑤ current-claims partial indexes (migration `20260716230000_perf_indexes.sql`); ⑥ fact-source snippets capped 280 chars | pure `lib/datamodo/extract-gate.ts` (+`tests/extract-gate.test.ts`), gate wiring in `extract.ts` `runExtractionForItem`, system-prefix in `extractFromMessage`, `CACHE_SYSTEM_ABOVE_CHARS` in `lib/llm/anthropic.ts` (+test) + mock, floor/cap + fast path + snippet cap in `knowledge.ts`, `app/api/search` |
| **Agent lenses, phase 1 (2026-07-16, GRAPH_PIPELINE.md P5)** — ONE graph per user with per-agent READ views: `facts.agent_id` (migration `20260716220000_fact_agent_lens.sql` — partial index + one-time backfill from `items.meta`); the extraction tick stamps body+attachment facts in one fail-soft UPDATE after ingest (attribution reads back off the item's addressed/routed meta); `listKnowledge(orgId, {agentId})` is the lens chokepoint (entities never split — a lens just shows fewer facts); `?agent=` on `/api/knowledge/entities` and `/api/search` (knowledge evidence, GraphRAG traversal, and grounded answers scope to the lens; table-row keyword hits stay global). Phase 2 open: UI lens chips, agent-addressed chat defaulting to its lens, MCP params | migration + `neon/schema.sql` + `prisma/schema.prisma`, stamp in `extract.ts`, lens in `knowledge.ts` `listKnowledge`, params in `app/api/knowledge/entities` + `app/api/search` |
| **Template block + self-creating templates (2026-07-16, user decisions)** — ① template FIELDS are now a GUARANTEE: every ingest ends with each templated node carrying at least its template fields, null-filled when unknown (`ensureTemplateSlots` — placeholder facts, confidence 0, race-safe via skipDuplicates; prompt adds "ATTEMPT EVERY template field"); the first real value fills a null slot SILENTLY (no fact_conflict — completion, not contradiction); extra metadata beyond the template stays welcome. Placeholders render "—" and are excluded from orphan edge-counts (consolidate + orphan-accept), ontology-health usage, and adjudication fact context. Net: tables can always just read the metadata. ② `category_proposal` drafts are grounded in the user's world: observed literal predicates ("prefer fields matching these"), observed EDGES grouped by target kind (deterministically merged into the drafted relations), business context, agent purposes — entities sharing metadata "by chance" become a proposed template whose relations are inferred from the graph | `ensureTemplateSlots` + placeholder-fill rule in `knowledge.ts`, guards in `consolidate.ts`/`reviews.ts`/`analytics.ts`, prompt line in `promptKindTemplate` (`ontology.ts`), context-grounded drafting in `maybeProposeCategories` (`kinds.ts`); decision recorded in MEMORY.md |
| **Document extraction, context-rich (2026-07-16, user calls)** — ① the distill prompt now carries: the relevance-primed known-entities block computed on the DOCUMENT's own text, the user's active AGENTS (name + purpose — "a dropped PDF usually exists for one of these"), and their TABLES (name + column keys — matching predicates land straight in the table); ② `DOC_SYSTEM`'s summary is now a full markdown-page style guide (overview → `##` sections → `\| field \| value \|` table → bold figures → `[[wikilinks]]` around extracted entities, rendered live by the entity page) — the ≤3-concepts cap stays (it caps TAGS, not content); ③ **scanned-PDF vision is OPT-IN** (`PDF_SCAN_VISION=1`; default off per user call "no vision on PDFs for now" — scans stay metadata_only, requeue re-reads them after enabling; photos unaffected); ④ merge **adjudication is metadata-aware**: candidates carry natural keys + support + top-4 current facts (`enrichMatchCandidates`/`topFactsForEntities`), the consolidation sweep passes both sides' facts, and the judge prompt weighs identifiers as near-proof/near-disproof | prompt blocks in `buildDocumentPrompt` (`ontology.ts`), style guide in `DOC_SYSTEM` (`extract.ts`), context loads + gate in `documents.ts`, enrichment in `knowledge.ts` + `consolidate.ts` |
| **Relevance-based entity priming (2026-07-16, user call — GRAPH_PIPELINE.md P2.6)** — the extraction prompt carries graph entities chosen BY the input, replacing the static top-30 concept list: lexical leg (labels literally in the message, any support) + semantic leg (ANN over ONE embedding of the message head, current space, sim ≥ .3, support ≥ 2 for non-concepts), merged/capped 15. Non-concepts render as the "ALREADY IN the user's graph" block — labels only, never ids, with the never-force rule so uncertain identity stays in the resolution ladder + review gate. Concepts stay their own leash line (primed-first, support-ranked fill to 12 — never empty without embeddings). `EXTRACTION_VERSION` → 4 (requeue optional). Messages only; documents = follow-up | pure `lib/datamodo/priming-core.ts` (`rankPrimedCandidates`/`conceptsForPrompt`/`renderKnownBlock`, +`tests/priming.test.ts`), DB legs `lib/datamodo/priming.ts`, wiring in `buildUserPrompt`/`runExtractionForItem` (`extract.ts`) |
| **Zero-cost agent routing (2026-07-16, user call)**: an UNADDRESSED item in auto mode routes to the best-matching ACTIVE auto agent — a pure lexical classifier (name+purpose keyword profiles, idf-discounted shared terms, name ×2, plural folding; absolute floor + margin over runner-up; **ambiguity never routes** → generic agent). No LLM, no spend. Explicit `@agent` always wins; routed picks steer extraction with that agent's purpose and stamp `meta.routed_agent_id/name/terms` (chat GET returns `routedAgent`). Verified E2E on the packed artifact (invoice→Bookkeeper, interview→Recruiter, off-topic→generic, explicit beats router). **Live suggestion while typing (same day)**: the router also runs client-side on every keystroke — an unaddressed draft matching one agent shows a "↪ Agent? Tab" chip in the composer's to-row (click/Tab = explicit recipient, ✕ dismisses; sticky recipient suppresses); sent-anyway messages show the ↪ routedAgent attribution chip on the bubble. Verified 7/7 (Playwright on the packed artifact). **ADAPTIVE (2026-07-17, GRAPH_PIPELINE.md "Adaptive classifiers (1)")**: routing is a decision WITH feedback, so it learns from acceptance — a `routing_events` log ties message heads to agents (an explicitly addressed send = ground-truth accept, logged in `/api/chat`; the suggestion chip's ✕ = explicit reject via `POST /api/chat/routing`; an auto-routed item analyzed >24 h with no correction = implicit accept, swept by the nightly tick); the consolidation tick's pass ⑤ embeds the accepted heads (one batch) and folds a PER-AGENT CENTROID (incremental mean, memory-capped 200, restarted on embedding-space change) + `routing_terms` weight corrections (accepts push a message's terms up, rejects down, clamp ±1, top-24 kept) onto the agents row; the router then scores lexical overlap (learned corrections folded into the profile — negative weights veto) + `centroidBoost(cosine(message, centroid))` (floor .3, cap 2, needs ≥5 folds). Still zero LLM: the ONE message embedding is shared with relevance priming (computed once in `runExtractionForItem`, passed to both). Ambiguity still never routes. All fail-soft pre-migration (`20260717090000_routing_feedback.sql` owed on dev+prod). SQL live-verified against embedded Postgres (migration idempotency, implicit-accept dedupe, centroid round-trip, boost query) | pure `lib/datamodo/agent-router.ts` (`learnedTerms`, `boosts`; +`tests/agent-router.test.ts`) + `lib/datamodo/routing-learn.ts` (`foldCentroid`/`learnTermWeights`/`centroidBoost`, +`tests/routing-learn.test.ts`), DB shell `lib/datamodo/routing.ts` (`logRoutingEvent`/`agentRoutingBoosts`/`agentLearnedTerms`/`learnRoutingProfiles`), hook + shared embedding in `runExtractionForItem` (`extract.ts`), vector pass-through in `priming.ts`, accept log in `app/api/chat`, reject route `app/api/chat/routing`, dismiss hook in `app/dashboard/chat-view.tsx`, pass ⑤ in `consolidate.ts`, migration `20260717090000_routing_feedback.sql` |
| Document extraction: classify-first, template-restrained, markdown summary (thick node). **Interpretable filenames (2026-07-16)**: a machine-named upload (scan0001.pdf, IMG_1234.jpg, UUIDs, "document (3)") is renamed `<kind>-<primary subject>.<ext>` once understood — applied to the attachments row, the doc entity label/natural key, review labels, the parse reply; human-looking names NEVER touched; original kept as an `original_filename` fact; re-runs stable (renamed = no longer cryptic). Verified 3/3 E2E | `classifyDocumentKind`/`extractFromDocument` in `extract.ts`; pure text/chunk core + `isCrypticFilename`/`interpretableFilename` in `lib/datamodo/document-extraction.ts` (+tests); orchestration + rename hook `lib/datamodo/documents.ts` |
| **Vision tier**: image attachments → understood thick nodes (1 vision call; fail-soft). **Scanned-PDF OCR (2026-07-14; MULTI-PAGE 2026-07-16)**: a PDF with an empty/near-empty text layer (`isLikelyScannedPdf`, < ~24 chars/page) is rasterized — `rasterizePdfPages` renders page 1 up to `MAX_SCAN_PAGES` (6) within a base64 payload budget (`unpdf` `renderPageAsImage` + native `@napi-rs/canvas`) — and ALL pages ride ONE vision call (`extractFromImage additionalPages`; prompt: "consecutive pages of ONE document"); pages dropped by the caps → `truncated` → indexing "partial"; a page-N>1 render failure keeps pages 1..N-1; fail-soft to `metadata_only` without a canvas/key. Verified 7/7 E2E on the packed artifact (mock logs the image-part count) | `extractFromImage` in `extract.ts`, `buildImagePrompt` in `ontology.ts`, `attachmentImageType`/`isLikelyScannedPdf`/`rasterizePdfPages` in `document-extraction.ts` (+tests), scanned branch in `documents.ts`, `serverExternalPackages` in `next.config.ts`, providers `lib/llm/*` |
| **Audio tier**: audio attachments → transcript (fail-soft Whisper-shaped client) → classify-first document pipeline → thick node (body = summary + capped transcript; full transcript in doc_chunks) | `lib/llm/transcription.ts`, `attachmentAudioType`/`buildTranscriptBody` in `document-extraction.ts`, audio branch in `documents.ts` |
| Spreadsheet attachments (.xlsx → text → same pipeline) | `sheetToText` in `document-extraction.ts`, `lib/datamodo/spreadsheet.ts` |
| **PDF → markdown converter seam + section-aligned chunking (2026-07-16, GRAPH_PIPELINE.md P3 phase 1)** — PDFs try an EXTERNAL structure-preserving converter first when `PDF_MARKDOWN_COMMAND` is set (any Docling/marker/MinerU-style CLI; contract: temp PDF path appended as last arg, markdown on stdout; `PDF_MARKDOWN_TIMEOUT_MS` default 45 s; output < 40 non-ws chars = scan = failure). Fail-soft: seam off / converter error → the unpdf text layer exactly as before, scanned-PDF vision detection intact. Markdown (≥2 headings, `looksLikeMarkdown`) chunks PER SECTION with the heading carried on every piece — citations name the section (markdown loses page lineage; headings replace it). Unit-tested via fake converter scripts. Phase 2 open: package a real converter per deployment + live-fire on a structured PDF | seam `lib/datamodo/pdf-markdown.ts` (+`tests/pdf-markdown.test.ts` + `tests/fixtures/fake-pdf-md*.mjs`), `looksLikeMarkdown`/`chunkMarkdownSections` in `document-extraction.ts`, hook in `documents.ts` |
| Generated notes: substantive prose dump → note node WE author (`note:`/`memo` forces) | `buildNoteExtraction` in `document-extraction.ts`, note logic in `extract.ts` |
| Ontology/categories: user-editable kind registry, prompt steering, canonicalization (fixes predicate drift), template restraint | pure `lib/datamodo/ontology.ts`; DB `lib/datamodo/kinds.ts`; API `app/api/kinds*`; UI `app/dashboard/categories-modal.tsx` |
| **Extraction reconciliation + declared cardinality (2026-07-17, user call: "author: xxx, author: yyy — only the last wins"; everything zero-LLM)** — ① a deterministic pre-fold pass runs inside `ingestExtraction` for EVERY path (messages, docs, images, notes, obsidian, MCP, off-template replays): facts referencing unknown/self localIds DROP instead of throwing the whole item into retry (which used to burn extra LLM calls), exact repeats collapse (max confidence), and several distinct same-message values for one predicate become coexisting `"many"` facts — never a last-one-wins supersession chain + junk `fact_conflict` reviews; ② cardinality is now DECLARED on the registry (`KindField`/`KindRelation.cardinality`; fields default `"one"`, relations default `"many"`; builtins: author/email/phone → many, issued_by/billed_to → one) and enforced deterministically — a declared-`"one"` slot with conflicting same-message values keeps the most confident only; prompts render `, list`/`(single)` hints; ③ cross-message alignment: a slot already stored as a list turns new singles into list members, and an old bare-key `"one"` fact migrates to list form (placeholder → retires) when a list arrives; `ensureTemplateSlots` counts many-form facts as filled (no placeholder resurrection); ④ typed-value rescue in `toExtraction`: number/date valueTypes with the value left in `valueText` ("$1,234.56", "March 3, 2026") recover the typed value (whole-string matches only), confidences clamp to [0,1]; DOC/IMG prompts finally teach cardinality (message prompt always did) | pure `lib/datamodo/reconcile-core.ts` (`reconcileExtraction`/`declaredCardinality`/`valueSlot`/`parseLooseNumber`/`parseLooseDate`, +`tests/reconcile.test.ts`), wiring + `alignSlotCardinality` + slot-fill fix in `knowledge.ts`, cardinality on `KindField`/`KindRelation` + prompt hints in `ontology.ts`, sanitize in `kinds.ts`, rescue/clamp + prompt rules in `extract.ts` |
| **Extraction hardening tier 2 (2026-07-17, user call: "anything free that improves extraction" — all zero-LLM)** — ① VALUE-EQUALITY normalization: text facts compare case/whitespace-insensitively in `upsertFact` ("Paid" restating "paid" = re-observation + provenance, not a supersession + junk conflict review; first-seen casing kept for display), date values canonicalize to padded YYYY-MM-DD in reconcile (uncomparable "2026-1-2" vs "2026-01-02" claim slots gone; garbage dates drop the fact), blank text / NaN numbers drop (`droppedEmpty`); ② NATURAL-KEY canonicalization in reconcile: emails lowercase, phones digits-only with leading + (`normalizePhone`), URLs de-tracked (utm_*/fbclid/gclid stripped, host lowercased, trailing slash dropped — also applied to url/website/link fact values) — the same real-world key now always lands on the same tier-0 `normalized_key`, killing duplicate entities resolution could never see; ③ EVIDENCE GROUNDING (`groundExtractionEvidence`): each fact's claimed evidence is verified against the text the model read — a quoted snippet appearing nowhere, or a ≥3-digit number absent from the source's digit stream (separators ignored), caps that fact's confidence at 0.35 and the extraction's overall at 0.7 (under the 0.75 review gate → files an extraction review); absence of evidence never penalizes, presence always passes (no false positives by construction); applied in message + document extraction after the escalation decision (never spends a second call); ④ SUPERSESSION GUARD: a clearly weaker claim (confidence >0.2 below current, or below a ≥2-source corroborated current) no longer silently overwrites — the candidate is recorded RETIRED with provenance and the `fact_conflict` review files as `held` (old value stays current; ACCEPT applies the switch old→new, REJECT closes with zero store change; Studio note + channel ping say "switch to it?"); non-held conflicts keep the applied-then-confirm flow | `textEq`+guard+`held` outcome in `upsertFact`, `factsHeld` in `IngestExtractionResult` (`knowledge.ts`); `canonicalizeNaturalKeys`/`normalizePhone`/`normalizeUrl`/`canonicalDateValue`/`normalizeFactValue`/`groundExtractionEvidence` in `reconcile-core.ts` (+7 new tests); grounding hook + `GROUNDING_OVERALL_CAP` in `extract.ts`; held accept/reject branches in `reviews.ts`, `held` on `ConflictReview` (`review-types.ts`), held wording in `review-ping.ts` |
| AI-drafted category templates + one-click category→table (**structural binding**: `datasets.kind_id` → `kinds.id`, set on create/adopt; plural-name match is only a pre-migration fallback) | `suggestKindTemplate` in `kinds.ts`, `app/api/kinds/suggest`, `app/api/kinds/[id]/table`, `datasetForKind` in `app/dashboard/schema-view.tsx`, `neon/migrations/20260711100000_datasets_kind_id.sql` |
| Off-template review routing (drops become reviews; accept = replay through ingest) | `restrictExtractionToTemplates`/`buildOffTemplateReview` in `ontology.ts`, `createOffTemplateReview` in `knowledge.ts`, accept in `reviews.ts` |
| `extraction_version` stamp + delta requeue endpoint | `EXTRACTION_VERSION` in `extract.ts`, `app/api/jobs/extract-requeue/route.ts` |

### Knowledge layer (canonical store)
| Feature | Code |
|---|---|
| Entity resolution: exact key → trigram blocking → ANN (embeddings) recall → LLM adjudication w/ confidence policy (auto ≥.85, propose ≥.55) | `resolveEntity`/`adjudicateMatch` in `lib/datamodo/knowledge.ts` |
| Facts: append-only, bitemporal, claim-key dedup, contradiction → supersession | `upsertFact`/`ingestExtraction` in `knowledge.ts` (tables `entities`, `facts`, `fact_sources`) |
| Embeddings (fail-soft, 1536-dim, stored on entities + doc_chunks) — **one space per deployment**: every vector stamped with `embedding_model`, ANN recall filters to the current space, `POST /api/jobs/embed-requeue` (CRON_SECRET) re-embeds stale/missing rows after a model change | `lib/llm/embeddings.ts` (`embeddingsModel`), stamp+gate in `knowledge.ts`/`chunks.ts`, `app/api/jobs/embed-requeue`, migration `20260711110000` |
| Document evidence layer: page-lineage chunks + passage search — keyword recall UNION **semantic ANN recall (2026-07-14)** over chunk embeddings (fail-soft: keyword-only without a key; ANN filters to the current `embedding_model` space; semantic hits extend recall but never outrank exact matches — both-paths passages get boosted) | `chunkDocText` in `document-extraction.ts`, `lib/datamodo/chunks.ts` (table `doc_chunks`), pure merge `lib/datamodo/passage-rank.ts` (`mergePassages`) |
| Entity merge (repoints facts/chunks/rows/body, tombstones) | `mergeEntities` in `knowledge.ts` |
| **Background consolidation (2026-07-16, GRAPH_PIPELINE.md P1)** — the graph's homeostasis pass, daily cron: ① embedding backfill (missing/stale-space vectors, batch-capped); ② merge sweep — trigram + ANN same-kind candidate pairs (recall only), pairs already ruled on (any entity_merge review, either direction) and natural-key-conflicting pairs excluded, the rest LLM-adjudicated on the owner's provider (BYOK-aware, fail-soft → propose-only, never text-only auto-merge): ≥.85 auto-merges (winner = more edges > support > older; loser's missing natural keys accrete first; logged as accepted review), ≥.55 proposes, below auto-rejects so the pair never re-asks; ③ orphan pass — support≤1 + zero edges + no body + older than 30d (concepts 7d; document/note never) → ONE batched `orphan_prune` review per tick, ids excluded from future asks whatever the decision. Accept prunes only entities STILL unlinked at accept time; decline never re-asks. Budget-capped per org, idempotent | pure `lib/datamodo/consolidate-core.ts` (+`tests/consolidate.test.ts`), orchestration `lib/datamodo/consolidate.ts`, `app/api/jobs/consolidate` (CRON_SECRET), `.github/workflows/consolidate-cron.yml` (daily 04:15 UTC), orphan card in `review-{types,card,studio}`/`reviews.ts`/`review-ping.ts`; env knobs `CONSOLIDATE_ADJUDICATIONS`/`CONSOLIDATE_EMBED_BATCH`/`CONSOLIDATE_ORGS` |
| Review queue: merges, conflicts, low-confidence extractions, off-template, **category proposals (growth loop ⑤: ≥3 entities of an unknown kind → proposed category with AI-drafted template; accept creates it, decline never re-asks)** — impact-ranked, accept/reject with real side-effects | `lib/datamodo/reviews.ts`, `lib/datamodo/review-types.ts`, `app/api/knowledge/reviews*`; trigger `unregisteredKinds` in `ontology.ts` + `maybeProposeCategories` in `kinds.ts` |
| **Channel pull-requests (2026-07-12)**: when a message leaves decisions behind (reviews / proposed rows) — and ONLY then; confident extractions ping nothing — the pipeline pings the user back over the channel it arrived on, numbered newest-first ("datamodo — 2 things need your OK: 1. Merge …? 2. Create category …? Reply '1 yes' / '2 no'"); replying "1y"/"no 2"/bare "yes" in WhatsApp **or Slack (2026-07-16)** resolves the review with real side-effects and answers with a confirmation (Slack: text-only DMs from bound senders run `applyReviewReply` before capture, confirmation via chat.postMessage; messages with files always capture); long/normal messages are never swallowed (strict short-reply parser). Outbound is env-gated fail-soft: WhatsApp via Twilio (`TWILIO_WHATSAPP_FROM`), Slack via `SLACK_BOT_TOKEN`, **email via Resend (2026-07-16** — `RESEND_API_KEY` + `EMAIL_FROM`, `RESEND_BASE_URL` override; email has no reply loop, so its ping says "Review at <url>" — `buildReviewPing replyable:false`, only whatsapp/slack marked replyable**)**; Teams has no outbound sender yet (dormant — Review tab still shows everything) | pure `lib/datamodo/review-ping.ts` (`buildReviewPing`, `parseReviewReply`, `reviewQuestion`); shells `review-inbox.ts` (`pendingQuestions`, `applyReviewReply`), `outbound.ts` (+`tests/outbound-email.test.ts` against a mock Resend); hook at the end of `runExtractionForItem` (`extract.ts`); reply interception in `app/api/webhooks/whatsapp` + `app/api/webhooks/slack` (+ `getBoundSource` in `channels.ts`) |

| **Research adoptions P2.5 (2026-07-16, GRAPH_PIPELINE.md §10b)** — ① alias-aware growth gate: an off-template predicate whose tokens subset/Jaccard-match a template key proposes an ALIAS of that key (`predicatesLookAlike`; `field_proposal.detail.aliasOf`) — accept appends the alias so canonicalization collapses every future write; existing facts keep the old spelling (predicate migration later). ② usage-weighted retention: retrieval stamps `entities.last_used_at` (`touchEntities` — graph seeds + answer citations in the search route, dossier downloads); orphan flagging AND orphan-accept skip anything read inside the prune window. Readers use `to_jsonb(e)->>'last_used_at'` → fail-soft before **migration `20260716210000_entity_usage.sql`** is applied. Follow-ups: entity-page-open beacon, MCP-read stamping, embedding-level predicate snap | `predicatesLookAlike`/aliasOf in `ontology-health.ts`, alias accept in `reviews.ts`, `touchEntities` in `knowledge.ts`, stamps in `app/api/search` + `entities/[id]/dossier`, guards in `consolidate-core.ts`/`consolidate.ts`/`reviews.ts`, migration + `neon/schema.sql` + `prisma/schema.prisma` |
| **Ontology-health telemetry + template growth gate (2026-07-16, GRAPH_PIPELINE.md P2)** — vocabulary sprawl made visible and actionable: per-kind **Ontology Conformance** (share of current facts whose predicate is template vocabulary — fields+relations+aliases+universal predicates like `mentions`; null for unregistered kinds), **new-predicate rate** (first-EVER appearance inside the window, superseded rows included), distinct-predicate counts, top off-template predicates. Surfaced as the "Ontology health" Insights card (worst-conformance-first bars + off-template chips + new-this-week). Growth gate: the consolidation tick files a `field_proposal` review when ≥3 current facts on a registry kind share an off-template predicate (majority value type; entity-valued → relation; majority unit; ≤2 proposals/kind/tick; kind+predicate never re-asked whatever the decision) — accept appends the field/relation to the kind template via `updateKind` (idempotent), decline never re-asks. Prior-art notes in GRAPH_PIPELINE.md §10b | pure `lib/datamodo/ontology-health.ts` (+`tests/ontology-health.test.ts`), DB shell `loadHealthFacts` in `analytics.ts`, `op:"ontology_health"` in `app/api/knowledge/analytics`, card in `insights-view.tsx`, pass ③ in `consolidate.ts`, review kind in `review-{types,card,studio}`/`reviews.ts`/`review-ping.ts` |

### Projections (every view derives from the vault; nothing is a second store)
| Feature | Code |
|---|---|
| Tables: facts → dataset rows (auto-materialized, human edits protected) + full manual/import/versioning layer | `lib/datamodo/project.ts`, `lib/datamodo/datasets.ts`, `lib/datamodo/review.ts` |
| Knowledge cards (facts, provenance drill-down, completeness cues) | `app/dashboard/knowledge-view.tsx` |
| ~~Knowledge graph (Map)~~ REMOVED 2026-07-11 (user: redundant next to the walk) — `entities.graph_pin` + its PATCH endpoint remain dormant | (view deleted; endpoint `PATCH app/api/knowledge/entities/[id]` kept) |
| **Explorer v2 — the 3D graph walk**: depth-field ego graph (center forward, hop-1 ring, hop-2 hazy behind cream fog, plus a decorative THIRD ring of deep unreadable card silhouettes for atmosphere — `GhostRing`, non-interactive, no edges, 3D-only), world-reflow walk with enter-from-parent/recede animations, floating breadcrumb + jump box, edge inspector (confidence meter · since · corroboration pips · quoted evidence), natural-shape side panel; flat 2D radial under `prefers-reduced-motion`. **Ring grouping (2026-07-12, WOW build-order step 1)**: one kind can't hog the ring — past `maxPerKind` (3) the tail folds PER KIND into a dashed "+N more invoices" pseudo-node (majority-predicate spoke, nothing silently dropped, cited nodes never folded); click expands the member list, click a member to walk to it | pure `lib/datamodo/explorer.ts` (`buildEgoGraph` + `clusterTail`/`maxPerKind`/`clusterOf`, `parentOf`, `depthLayout`, `DEPTH`), `app/dashboard/explorer-view.tsx` (`ClusterPanel`); design source: Claude Design project "Datamodo Explorer v2" |
| **Explorer zoom-out — layers (2026-07-13, FINAL shape after two discarded physics iterations)**: scroll out on the walk and the SAME center's world unfolds as concentric BFS rings — hop 3, 4, 5… (the walk shows only 2), plus one dashed final ring of entities not connected to the center. ONE dial, zero physics: every node holds a PERMANENT bearing (deterministic wedge subdivision — each subtree owns an angular slice of its parent's wedge, blended 55% subtree-weight / 45% uniform so heavy branches can't squeeze siblings into slivers; ring spacing adapts to the busiest ring but never to the zoom), so zooming is a pure rescale — no wiggle is geometrically possible, and a node that leaves the view reappears at the exact same spot. Per-parent tails fold into "+N more" chips at every depth (the walk's ring-grouping rule); cards are the walk's own `NodeCard`s scaling ∝ the scroll — walk-sized at 2 layers on ring radii that MATCH the walk's rings, and the WALK itself sits its cards at the layered bearings (`layeredAngles` + `depthLayout` override; kind-chips take their members' circular mean), so scrolling out is a pure continuation: same angles, same rings. Zoom is CONTINUOUS (Feature 2, 2026-07-14 — REVISES the 2026-07-13 "DISCRETE" call): scroll feeds a TARGET float ring count and a rAF loop EASES the displayed `layers` toward it (~0.2/frame, ~300ms settle), so the reveal is a smooth animation — cards and edges travel together and the new ring's edges draw outward — instead of a per-tick jump (the first cut mapped wheel deltas straight to `layers`, which read as jumps; smoothed same day). SENS ≈ one ring / 300px. `floor(zoom)` rings are landed; the fraction emerges the next ring FROM THE CENTER (radius 0 → target, always full-blurred) with its edges drawing outward (the emerging edge's outer endpoint travels out, so it "draws" center→rim; opacity ramps with the emergence). The geometry is the ORIGINAL per-integer wheel computed for `floor` and `floor+1` and LERPed by the fraction (`wheelGeom(KK)` in `LayeredView`) — so the landed rings smoothly shrink inward (camera pulls back) while the old blurred frontier "becomes less blurred and bigger" for free (ring K is the blurred frontier at count K but a sharp interior at count K+1, so the LERP does it). The scroll HOLDS wherever you stop — no auto-snap. The walk↔layered boundary (zoom < 2) is still a swap (3D perspective vs 2D wheel); everything from 2 rings up is continuous. EVERY ring obeys the base view's rules RELATIVE to the current view: only the OUTERMOST ring (the frontier) is blurred (hop-2's haze — no gradient, so everything revealed stays readable however far out), and the HIGHLIGHTED layer is the last sharp ring — the one just revealed: biggest cards (coral ring badge), sizes tapering DOWN toward the center (0.8×/ring). Ring radii derive from the card heights sitting on them (focus ring gets the clearance its big cards need; tapered inner rings pack tight) then fit the canvas — radial overlap impossible by construction, bearings still fixed. Angular spacing: busy rings blend toward even full-circle spacing (∝ fullness; sparse rings keep parent-wedge locality) with a hard half-slot minimum gap between neighbours, and the crowd factor is capped relative to the focus ring so the highlighted layer is always the biggest on screen. The emerging ring surfaces continuously from the center as you scroll (small + transparent → grows + fades in, full blur); hover unblurs + pops a deep card readable; the layout tracks the scroll every frame (recenter still blooms every ring from the new center via `RingAnim.all`). Hover pops a mini-card to readable; **click any card → recenters on it AT THE SAME zoom level (2026-07-14): it stays in the layered view and blooms the new center's rings, rather than snapping back to the fully-zoomed-in walk**; scroll below the entry step → the walk. **The ring "N hops"/"not linked yet" text badges were removed (2026-07-14) — the concentric card rings carry the depth on their own — and the edges are soft arcs bowing toward the center (control point pulled 40% toward origin → a hierarchical-bundling look that belongs to the circular layout) instead of grey straight chords. Edges are CLICKABLE at every zoom level (Feature 1, 2026-07-14): a fat transparent hit path opens the SAME fact inspector as the walk (the light `{from,to,predicate}` layered edge is rebuilt into a full `EgoEdge` from the subject's facts on click; cluster spokes expand instead). NO predicate labels are drawn on edges at ANY zoom level (2026-07-14, revised from "base keeps them"): the walk's `EdgeLayer` no longer renders the predicate `<text>` — the name shows only in the fact inspector on click.** Earlier same-day iterations (force-graph constellation Map with cluster LOD + physics, then a calm pass) are REPLACED — user call: "zoom on one axis only, fixed positions, no wiggle"; the `constellation.ts` clustering/LOD core stays dormant for the COSMOS | pure `buildLayeredEgo` in `lib/datamodo/explorer.ts`, `LayeredView` + wheel dial in `app/dashboard/explorer-view.tsx`, tests in `tests/explorer.test.ts`, harness `scripts/shoot/harnesses/explorer-layers.tsx`; dormant: `lib/datamodo/constellation.ts` (+ `tests/constellation.test.ts`) |
| **Whole-vault Graph (⊛ Graph sub-tab, 2026-07-14 — the roadmap's sigma spike, PARALLEL to the Explorer, keep/kill call open; custom-rendering pass same day)**: the entire vault on one WebGL canvas — every entity a tiny CARD (custom node program: rounded rectangle, paper fill, kind-colored frame — the walk's card grammar in miniature; matching canvas-2D label/hover drawers), every relationship a CURVED edge (`@sigma/edge-curve`); NOT centered on one node (that's the walk's job). graphology holds the graph, sigma.js renders it; the user picks the layout — ✦ Organic (ForceAtlas2) · ◯ Circle · ◉ By kind (circlepack) — all deterministic (circular seed, zero randomness). Hover/click lights the neighborhood coral and dims the rest; **clicking a card opens the Explorer's natural-shape SIDE PANEL on the right** (kind kicker, "◍ Walk" hand-off to the Explorer, "Full page ›", and the full `EntityPageBody` — record table, relationships, markdown body with `[[wikilinks]]` that recenter the graph + glide the camera); edge click opens a light link inspector listing every predicate on the pair, endpoints clickable (directions kept; no labels drawn on edges — standing rule). Kind legend + node/edge counts in the toolbar; datasets ride along as virtual nodes (same `buildDatasetNodes` projection as the walk). Heavy libs load lazily on tab open; fails soft to a message without WebGL. ⚠ Do NOT set sigma's `stagePadding` — a custom padding misaligns sigma 3.0.3's picking buffer and silently kills every node/edge click (repro'd in isolation) | pure `lib/datamodo/vault-graph.ts` (`buildVaultGraph`, reuses `buildAdjacency`), `app/dashboard/graph-view.tsx` (`LinkInspector`, side panel via `EntityPageBody`), `app/dashboard/graph-card-program.ts` (`NodeCardProgram`, `drawCardNodeLabel/Hover`), wiring `knowledge-view.tsx` (`view: "graph"`) + `control-center.tsx` (`DataView`); tests `tests/vault-graph.test.ts`; harness `scripts/shoot/harnesses/graph.tsx`; deps `graphology`, `graphology-layout`, `graphology-layout-forceatlas2`, `sigma`, `@sigma/edge-curve` |
| **Node shapes phase 2 + audio**: image documents render the original inline (`?inline=1` streaming), `bookmark` builtin kind renders a link card, datasets appear as walkable virtual nodes in the Explorer (`contains` edges to row entities), audio documents render a PLAYER streaming the original above the summary+transcript body | pure `lib/datamodo/node-shapes.ts` (`entityImageType`/`entityAudioType`); `app/dashboard/entity-page.tsx`, `knowledge-view.tsx`, `app/api/knowledge/entities`, `app/api/documents/[id]` |
| ~~Concept map view~~ REMOVED 2026-07-11 (concepts are just a kind — they live in the schema/tables surface); pure core kept dormant | pure `lib/datamodo/concept-map.ts` (+tests) |
| Timeline (messages · domain dates · corrections · first sightings; Upcoming; per-entity; sent-vs-arrived clock toggle; collapsed "◷ History" on every entity page) — design skin: vertical rail, coral day dots, rising cards, channel tints. **LIVES UNDER REVIEW since 2026-07-14** (Review = ALL change: ✓ Pending · ⎇ Commits · ◷ Timeline) alongside the **git-style Commit log**: one commit per extraction run, its facts as the diff (`+ added`, `~ was → now`), runs that wrote nothing aren't commits — a pure query over the bitemporal vault, nothing stored twice. **Per-entity BLAME (2026-07-14)**: the entity page's "◷ History" disclosure has a story ⇄ blame toggle — blame is this commit log filtered to the entity (`?view=commits&entity=`), so you see which run touched each of its facts | pure `lib/datamodo/timeline.ts` (`timeBasis`, `buildCommitLog`), `app/api/knowledge/timeline` (`?basis=sent`, `?view=commits`, `&entity=`), `app/dashboard/timeline-view.tsx` (`EntityHistory` story/blame, `CommitLogView`), Review toggle in `control-center.tsx` (`ReviewView`); harnesses `commits.tsx` + `entity-blame.tsx` |
| **Data tab IA** (flat + unified, 2026-07-11; ⊛ Graph added + Timeline moved under Review 2026-07-14): ONE toggle — ▦ Tables · ◍ Explore · ⊛ Graph · Files · Insights. **Tables = the unified surface**: a Supabase-style schema CANVAS — kind cards (columns + FK relation rows) are DRAGGABLE, relation lines follow live, bring-to-front on grab, arrangement persists (localStorage `dm-schema-positions-v1`), auto-layout seeds first visit; click a header (no drag) → browse rows as entity cards; "▦ open/make table" per card; "+ new table" in the toolbar creates category + dataset as ONE object; dataset list below | `app/dashboard/schema-view.tsx`, `knowledge-view.tsx` (`view: "schema"\|"explore"`), `control-center.tsx` (`DataView`) |
| Entity pages (record table / document summary page); bodies render **Obsidian-flavored markdown** — tables, images, task lists, quotes, code, ==highlight==/~~strike~~, `[[wikilinks]]` that resolve to real nodes (click = open), `![[embeds]]` rendering image/audio nodes inline, KaTeX math (lazy, MathML) — still injection-proof (pure AST → React, never HTML) | pure parser `lib/datamodo/markdown.ts`; renderer `app/dashboard/markdown.tsx` (`MarkdownBody`, `buildNodeResolver`); `app/dashboard/entity-page.tsx` |
| Dossier export (cited markdown download) | pure `lib/datamodo/dossier.ts`, `app/api/knowledge/entities/[id]/dossier` |
| **On-demand synthesis**: "✦ Synthesize" on entity pages (≥2 connected content bodies) → cited cross-document note stored as `body_md`; never automatic | pure `lib/datamodo/synthesis.ts`; `POST app/api/knowledge/entities/[id]/synthesize`; button in `entity-page.tsx`, wiring in `knowledge-view.tsx` |
| Files view (smart folders = projections over `mentions`) + original download | `app/dashboard/files-view.tsx`, `app/api/documents/[id]` |
| Insights (any measure × any axis, live aggregation) | `lib/datamodo/analytics.ts`, `app/dashboard/insights-view.tsx` |
| Search: tables + knowledge + document passages, grounded answers with citations. **Answers are GRAPH-FIRST (GraphRAG, 2026-07-14)**: the query links to seed entities (text coverage + ANN over `entities.embedding`), the fact graph around the seeds becomes the primary evidence (seeds' + neighbors' facts, seed-touching first — multi-hop/aggregation questions find evidence keywords miss), and semantic chunk retrieval is SCOPED to that neighborhood (one shared query embedding). Fails soft leg by leg to plain keyword evidence | pure `lib/datamodo/graphrag.ts` (`linkQueryEntities`, `expandFromSeeds`, `mergeKnowledgeHits`), `annLinkEntities` in `knowledge.ts`, scope/vector opts in `chunks.ts`, `lib/datamodo/search.ts`, `lib/datamodo/answer.ts`, `app/api/search`, `app/dashboard/answer-card.tsx` |
| **Search → graph ("see in graph")**: a grounded answer's citations resolve to graph nodes (entities, rows via `subject_entity_id`, passages via their document) AND the plain "In your knowledge" results carry the same button (works with NO LLM); one click opens the Explorer walk with those nodes HIGHLIGHTED — coral halo + ✦, lit edges between cited nodes, a "✦ used in the answer" chip row to hop cite-to-cite; cited nodes win ring slots (`EgoOptions.prefer`); node panel/edge inspector work as in the normal walk | `highlightIds` in `app/dashboard/explorer-view.tsx`, `prefer` in `lib/datamodo/explorer.ts`, `app/dashboard/answer-graph-modal.tsx` (`variant` answer/results), `onShowInGraph` in `answer-card.tsx` + knowledge-section button in `control-center.tsx`, `entityId` on `SearchHit` (`search.ts`/`answer.ts`) |
| **Derive a table from the graph** (Build ▾ → "Derive a table"): describe the table in plain language → LLM designs a spec over the graph's SCHEMA (kinds + predicates, never row data) → rows built deterministically from entities/facts/relations (attr values, linked labels either direction, counts) → PREVIEW with per-column source chips + honest counts → confirm creates the dataset (rows accepted, `subject_entity_id` linked, created_by `derive`) | pure `lib/datamodo/derive-table.ts` (`summarizeGraph`, `parseDeriveSpec`, `buildDerivedTable`), `app/api/knowledge/derive-table`, `app/dashboard/derive-table-modal.tsx` |
| **Folder lenses** (Files view — REPLACED the LLM folder modal 2026-07-12, user call: "folders derive deterministically, no LLM; suggest multiple trees"): a folder is a TAG derived from facts, so the same corpus organizes into MANY trees — by client · project · person · topic · month · type · channel; a doc linked to two clients appears in BOTH folders; only lenses that discriminate the corpus are offered; selecting a folder filters the doc grid; "↓ Export tree" downloads exactly the on-screen tree as .zip (originals from blob storage fail-soft to markdown, notes as markdown, + README; traversal-proof re-sanitizing server-side; multi-folder docs export in each). **Rebuilt as a true macOS Finder column view (2026-07-13)**: the cramped "tree box above the grid" became **Finder columns** — folders drill left → right into columns (every defined step always renders its column so its split label/header is visible immediately; a not-yet-drilled column shows an empty-state), and the TRAILING column shows the FILES in the selected folder (compact `FileRow`s: mono type badge + name + size + ↓, click opens the entity page). Its header does NOT repeat the folder's name (the folder is already selected/highlighted one column left) — it is reserved for the NEXT split: just a "+" that turns the shown files into sub-folders (the standalone "+" rail only appears when there's no content column to host it). The one twist vs Finder: each folder column's HEADER is its split control — the user decides how that level is classified via a searchable dimension picker (grouped Relationships / Concepts / Attributes, dedupes dimensions already used), and headers reorder (‹ ›) / remove (×). The classification is a USER-BUILT ORDERED PIPELINE of ANY depth (was a fixed 2-level "lens + then"); a small "+" rail at the far right adds a deeper split. Picker popover is FIXED-positioned (anchored to the clicked header) so it escapes the horizontally-scrolling column strip. With no splits, a single "All files" column lists everything. The old breadcrumb / search / DocCard grid below were removed — everything lives in the columns now. Engine: `buildLensTree` recurses by DEPTH INDEX (not `stack.indexOf`, which collapsed a repeated dimension) so pipelines nest to any depth. Lens engine otherwise unchanged (still projections, still "nothing is ever moved" — a drag-to-move tree was rejected as it contradicts the model) | pure `lib/datamodo/folder-lenses.ts` (`collectFiledDocs`, `lensValues`, `buildLensTree`, `treeToPlacements`) + `folder-export.ts` + `zip.ts`, `app/api/knowledge/folder-export` (plan→zip only), `app/dashboard/files-view.tsx`, harness `scripts/shoot/harnesses/files.tsx` |
| Review Studio (PR-metaphor queue). **One review-card core, two skins (2026-07-14)**: every review's evidence body (merge sides · conflict diff · extraction facts · off-template facts · category template) renders from ONE component under a paper skin (Studio) and an ink skin (the Chat bubble) — the chat's "needs your OK" bubble now carries full PR fidelity, and the two surfaces can't drift | `app/dashboard/review-studio.tsx`, shared core `app/dashboard/review-card.tsx` (`ReviewCardBody`, `PAPER_SKIN`/`INK_SKIN`), chat wiring in `chat-view.tsx` + `app/api/chat` (returns typed `ReviewItem`s) |
| Spreadsheet → knowledge graph import: **preview/confirm** (dry-run reading with per-column roles + honest counts; nothing writes unconfirmed), **column-mapping overrides** (kind · identity column · link/fact/skip · link target), **cross-row reference dedupe** (`combineExtractions`, batched ingest) | pure `lib/datamodo/infer-graph-core.ts`; shell `infer-graph.ts` (`previewTableGraph`), `app/api/knowledge/import-graph` (`mode=preview`, `overrides`), `app/dashboard/import-graph-modal.tsx` |
| Auto-link suggestions between tables; explicit dataset_relations draw as **dashed lines** between schema-canvas cards | `lib/datamodo/relations.ts`, `app/api/relations/suggestions`, `SchemaTableLink` in `app/dashboard/schema-view.tsx` |
| **Outbound sync — table → the user's own Postgres (2026-07-14, phase 1)**: one-way push, idempotent (upsert by the datamodo row id — re-sync converges, never duplicates). Pure SQL core generates quoted DDL (`dm_id` PK + typed columns) + a fully parameterized upsert (no interpolation); every connector is a WRITER behind one interface (Sheets/Drive/fs slot in later). "↑ Sync out" panel in the table editor; connection string used per-request, never stored. Live-verified against a real Postgres | pure `lib/datamodo/sync-postgres.ts` (`buildSyncPlan`/`rowParams`/`safeTableName`), shell `lib/datamodo/sync-outbound.ts` (`OutboundWriter`/`postgresWriter`, `pg`), `app/api/sync/postgres`, `SyncOutPanel` in `control-center.tsx`; dep `pg` |
| Queue pill: "⟳ processing N items" in the topbar (hidden when idle; fast-polls while draining; stuck items surface) | `app/dashboard/queue-pill.tsx`, `GET app/api/jobs/queue-status` |
| Onboarding / business context (steers extraction) | `app/dashboard/onboarding-modal.tsx`, `lib/datamodo/settings.ts` |
| BYOK (user's own OpenRouter/OpenAI/Anthropic key) | `lib/datamodo/llm-for-user.ts`, SettingsModal in `app/dashboard/ui.tsx` |
| **BYOK provider cost tracking (2026-07-14)** — what the user's OWN key cost while datamodo used it (NOT the datamodo plan): every LLM call reports token usage (`onUsage` hook in both providers), recorded to a `llm_usage` ledger; OpenRouter's EXACT per-call cost is stored (we ask for it), Anthropic/OpenAI are priced from token counts (pure `llm-cost.ts` list-price table, marked estimated), unknown models show tokens only, Ollama is free. Recording is fail-soft (a lost row never breaks extraction / survives an unmigrated table). Settings shows a "Your provider spend" card: 30-day total + per-model breakdown. Only tracks BYOK (cloud-mode calls run on OUR key = our cost). **Monthly spend cap (2026-07-16)**: `user_settings.byok_monthly_cap_usd` (Settings input, empty = none; Ollama-as-BYOK is keyless/free → no cap there); `llmForUser` checks month-to-date ledger spend (`monthToDateSpendUsd`, calendar month UTC, fail-soft) against the pure `byokCapDecision` BEFORE burning the key — at/over cap LOCAL falls back to the machine's free Ollama (logged), CLOUD fails the item with a clear requeue-able reason; the Usage card shows a cap progress bar + "cap reached — your key is paused". Verified 6/6 on the packed 0.2.0 artifact incl. the 0.1.0→0.2.0 vault `db push` upgrade | pure `lib/datamodo/llm-cost.ts` (`estimateCostUsd`/`byokCapDecision`), shell `lib/datamodo/usage.ts` (`recordUsage`/`usageHooks`/`usageSummary`/`monthToDateSpendUsd`), `LlmUsage`/`ProviderHooks` + capture in `lib/llm/*`, wired in `llm-for-user.ts`, `app/api/usage`, `UsageCard` + cap input in `control-center.tsx`; table `llm_usage` (migration `20260714120000`), cap column (migration `20260716170000`) |
| **MCP server (2026-07-14, phases 1+2; full tool surface + ships in BOTH editions 2026-07-16; DATAMODO MODE 2026-07-17)** — the vault as tools on the user's own Claude subscription: streamable-HTTP endpoint, identical code cloud + local (each bound to its own vault; verified 19/19 E2E on the packed local artifact). **Datamodo mode (2026-07-17, user ask "Claude chat as datamodo chat")**: the initialize response carries `MCP_INSTRUCTIONS` (when to engage · the filing loop · the answering loop · review etiquette — clients fold it into the system context, so Claude DECIDES when a turn belongs in the vault); the 15th tool `extraction_briefing(text)` returns the internal pipeline's whole steering in one call (`EXTRACTION_DOCTRINE` — the MCP twin of extract.ts's SYSTEM prompt, keep in sync — + categories/templates + relevance-primed known entities + concept leash + business context + agents); `submit_extraction` gained `note{title,body}` (same `buildNoteExtraction` path as chat: note node keyed to the source item, mentions/about edges, body_md page; noteless-source bounces before any write). Verified 18/18 live E2E on a served local instance (instructions on initialize, briefing shape, file→note→read-back via get_context/get_entity/list_facts, bounce files nothing, refile converges). **Guided connect card (2026-07-17, user ask "make adding the MCP to Claude easier — no JSON")**: the Settings "✦ Connect Claude" card auto-loads the connection (no reveal click) and is a copy-paste GUIDE, not a credential dump — server URL with a one-click "⧉ Copy URL" (Copied ✓ feedback), then three numbered steps: cloud = paste into claude.ai Settings → Connectors → Add custom connector (direct link) → Approve in browser (OAuth — "no JSON files, no tokens to paste"); local = same flow via the Claude DESKTOP app on this machine (claude.ai web can't reach localhost). The `claude mcp add` one-liner AND the bearer token fold behind a "▸ Claude Code & API" disclosure, so non-devs never meet a token. Verified 8/8 Playwright on a served local instance (auto-load, steps, disclosure hides/reveals, copy feedback, zero page errors) **Local is TOKENLESS (2026-07-16, user call)** — one user on 127.0.0.1, the port is the boundary; `/api/mcp-token` returns `authRequired:false` and the Connect card shows a header-less `claude mcp add`. Cloud keeps per-user HMAC bearer tokens. READ `list_kinds` / `search_entities` / `get_context` / `get_entity` (full record) / `walk_graph` (Explorer ego-graph: hop-1/2 + predicates) / `list_facts` (subject/predicate/kind filters + bitemporal **as-of**) / `search_documents` (chunk passages, keyword+semantic) / `list_tables` + `get_table_rows` (projections, read-only by design) / `pending_reviews` / `process_inbox` (raw `stored`/`failed` items + text — the PULL model: Claude extracts on the sub); WRITE `capture_message` (forward raw content → same ingest + extraction kick) / `submit_extraction` (strict zod contract; optional `itemId` attaches to a queued item, else source captured as an `upload` item `meta.via="mcp"`; either way the item is marked `analyzed` after the SAME deterministic `ingestExtraction` — Claude extracts, the server resolves/dedups/supersedes/reviews) / `resolve_review`. Auth: per-user HMAC-derived bearer tokens (no schema change; rotate `MCP_TOKEN_SECRET` to revoke); Settings → "✦ Connect Claude" reveals URL + token + the `claude mcp add` line. OAuth = phase 3 | `app/api/mcp/[transport]/route.ts`, pure `lib/datamodo/mcp-token.ts` + `lib/datamodo/mcp-extraction.ts`, `app/api/mcp-token`, `McpConnectCard` in `control-center.tsx`; deps `mcp-handler`, `@modelcontextprotocol/sdk`, `zod` |
| **Local edition — BYOK verified, per-provider model config (2026-07-16)**: "use your own Claude/OpenAI/OpenRouter key" works end-to-end locally — key saved in Settings (BYOK card → provider → key), extraction runs on that key, and **`llm.json` model names are namespaced PER PROVIDER** (`{url, providers:{ollama,anthropic,openai,openrouter}}`; legacy flat files migrate to `providers.ollama` on read) so the wizard's Ollama names never reach a cloud provider's API. `llmForUser` applies only the resolved provider's slot; `/api/local/llm-models?provider=`; `LocalAiFields` is provider-aware (per-provider placeholders; URL/status/datalist only for Ollama). Also: **`llm_usage` ships in `neon/schema.sql`** (local spend card was permanently empty) and **`usageSummary` fixed for cloud+local** (Prisma `groupBy._max` on a boolean emits `max(boolean)` → 42883; now raw SQL `bool_or`). Verified 11/11 on the packed tarball (key via real Settings UI → bearer on the wire → openai default model, no leak → per-provider override → slots isolated → ledger populates → toggle back to Local resumes keyless ollama) | pure `sanitizeLlmFile`/`LOCAL_AI_PROVIDERS` in `lib/local/config.ts` (+tests), `lib/local/llm-config.ts` (`readLocalLlmFile`/`writeLocalLlmModels`), `lib/datamodo/llm-for-user.ts`, `app/api/local/llm-models`, `LocalAiFields` in `control-center.tsx`, `usageSummary` in `lib/datamodo/usage.ts`, `llm_usage` in `neon/schema.sql`, keyed-request semantics in `scripts/mock-ollama.mjs` |
| **MCP OAuth — claude.ai connectors (2026-07-16, phase 3)**: datamodo is its own OAuth 2.1 **authorization server** for the MCP endpoint — claude.ai's "Add custom connector" now works with just the server URL: discovery (`/.well-known/oauth-protected-resource` + `/.well-known/oauth-authorization-server`, RFC 9728/8414, path-suffixed forms served, permissive CORS; the MCP 401 carries `WWW-Authenticate: Bearer resource_metadata=…`), **dynamic client registration** (RFC 7591 — public clients only, no secrets, PKCE binds the flow), branded **consent page** `/oauth/authorize` (client + redirect validated BEFORE render — bad pairs get an error card, never a redirect; signed-out users bounce through `/login?redirectTo=`; the Approve form carries ONE HMAC-signed field naming the exact grant shown, 10-min expiry — CSRF-proof), token endpoint `/api/oauth/token` (authorization_code + PKCE **S256 only**; single-use codes — DELETE-first so replays fail closed; **rotating refresh tokens**; RFC error shapes; `invalid_grant` never says which check failed). Tokens are opaque `dmo_`/`dmr_` random values stored **sha256-hashed** — a leaked table yields nothing; per-user revocation = delete the user's `oauth_tokens` rows (what the stateless HMAC tokens can't do) — surfaced in Settings as **"Connected apps" + per-app Disconnect (2026-07-16)**: `GET /api/mcp-token` returns `grants` (`listOAuthGrants` — live pairs grouped by client, fail-soft []), `DELETE /api/mcp-token?client_id=` runs `revokeOAuthGrants` (tokens + un-exchanged codes); verified a LIVE refresh token gets `invalid_grant` after Disconnect (22/22 OAuth E2E + Playwright UI pass). `resolveMcpBearer` (new `lib/datamodo/mcp-auth.ts`) accepts all three credentials: local tokenless · `dmk_` HMAC (Claude Code, unchanged) · `dmo_` OAuth — OAuth lookup is FAIL-SOFT so an unmigrated deployment keeps 1+2 working. Verified **19/19 E2E** on the packed artifact (discovery → register → consent → approve → exchange → refresh rotation → MCP `tools/list` under the OAuth bearer; negatives: forged consent, unknown client, redirect mismatch, code reuse, wrong verifier, wrong redirect, refresh replay, evil registration) + 9 unit tests (RFC 7636 test vector, consent signing, redirect rules). ⚠️ cloud needs migration `20260716150000_oauth.sql` applied | pure `lib/datamodo/oauth.ts` (+`tests/oauth.test.ts`), `lib/datamodo/oauth-store.ts`, `lib/datamodo/mcp-auth.ts`, `lib/datamodo/app-url.ts`, routes `app/api/oauth/{register,token,approve}`, page `app/oauth/authorize`, `app/.well-known/oauth-{protected-resource,authorization-server}/[[...rest]]`, challenge wrapper in `app/api/mcp/[transport]/route.ts`, tables in `neon/schema.sql` + `prisma/schema.prisma` |
| **Local edition — the tarball ships the PREBUILT app (2026-07-16)**: first `datamodo serve` boots in **~6 s** on a fresh vault (was: minutes of `next build` on the user's machine). `build:local-package --build` now prepares the production `.next` for shipping — strips what `next start` never reads (cache/trace/types/.nft.json), converts Turbopack's externalized-package **symlinks** (`.next/node_modules/pg-<hash>` → `node_modules/pg`; npm cannot pack symlinks) into `.next/local-externals.json`, and templates `required-server-files.{json,js}` (read by `next start` but embedding the build machine's app dir) with a path token. `bin/link-externals.mjs` reverses both **at postinstall** (a global install dir may be root-owned later) and again fail-soft on every `serve` (moved installs self-heal). Build env scrubs `NEXT_PUBLIC_*` (inlined into client bundles/prerenders — a CI machine's values must never ship) and a **leak sweep proves** no absolute paths / NEXT_PUBLIC values survive in the artifact. Deps are **pinned exact** from the installed tree (a `^` resolving a newer `next` on the user's machine would mismatch the shipped build). Tarball: 415 KB/184 files → **9.6 MB/944 files**. `serve`'s build-on-boot stays as the repo/dev fallback; `datamodo build` still does clean local rebuilds. Verified on the packed artifact: fresh install → links recreated → server-files materialized to the install dir → **5.8 s to a responding dashboard**, no build step in the log → 18/18 pipeline + 31/31 AI-panel E2E on that install | `prepareBuildForShipping` + exact pinning + leak sweep in `scripts/build-local-package.mjs`, `bin/link-externals.mjs` (NEW: `linkBuildExternals`/`materializeServerFiles`, chained in the generated postinstall), serve hook in `bin/datamodo.mjs` |
| **Local edition — non-dev AI settings panel (2026-07-16)**: everything about "manage my LLM" happens from the dashboard, never a terminal. Settings → Local card: live **"● Ollama is running · N models installed"** status (or a friendly "Ollama isn't running yet" box: download link → open app → **Check again** recovers in place); text/vision models are **dropdowns of what's actually installed**; models the first-run sizing recommends for THIS machine but aren't downloaded show as **one-click download chips** (`POST /api/local/ollama-pull` drives Ollama's pull; UI polls the installed list until the model lands); **"Test it"** fires ONE tiny real completion through the real provider stack and reports latency. BYOK card: **"Test key"** (free — provider's list-models endpoint) validates the typed key with human error copy on 401 ("rejected — double-check it… not your ChatGPT Plus/Claude Pro subscription") AND returns the models that key can use → the model fields become dropdowns too (Anthropic `/v1/models`, OpenAI `/v1/models` filtered, OpenRouter `/key`+`/models`); "Test it" proves the whole path key→model→answer. All "…is on the roadmap" copy removed. **Fresh-vault truth fix**: local `getSettings` reports the mode actually in effect — "byok" with no saved credential runs on the machine's Ollama (`llmForUser` fallback), so Settings now opens on "Local — on this machine" and the sidebar badge says "Local AI — this machine" instead of "Add your API key to start". Keys are used transiently by the probe, never stored. Verified **31/31** on the packed artifact (16 API + 15 Playwright UI: status→dropdowns→auto-save→Test it→kill Ollama→friendly box→Check again recovers→chip downloads a model→Test key→BYOK dropdowns→bad-key message) | `app/api/local/llm-probe` (NEW), `app/api/local/ollama-pull` (NEW), `recommended` tier in `app/api/local/llm-models`, `LocalAiFields` rewrite + `McpConnectCard`/BYOK copy in `control-center.tsx`, `effectiveSettings` in `lib/datamodo/settings.ts`, `/v1/models`+`/v1/key` in `scripts/mock-ollama.mjs` |
| **Local edition — code separation + Docker + installers (2026-07-16)**: the ROADMAP "HARD REQUIREMENT" shipped as option C (build-time prune). **Boundary lint** — `.dependency-cruiser.cjs`: CORE (dashboard, core/local API routes incl. MCP, `lib/{datamodo,llm,local,ingest}`, blob-fs, bin) must not import CLOSED (Neon Auth, `@neondatabase/*`, `@prisma/adapter-neon`, stripe, `@aws-sdk`, billing/webhooks/auth routes, landing); the 5 seam files (`lib/prisma.ts` · `lib/storage/blob.ts` · `lib/auth/session.ts` · `app/auth/actions.ts` · `proxy.ts`) are the only crossings; `npm run lint:boundary` + CI. **Prune** — `npm run build:local-package`: core → `dist/local-package/` with the seams swapped for local implementations (`packaging/local/overrides/`), generated `package.json` (name **datamodo**), then proves separation (grep sweep + zero-exception depcruise + `--build` full next build in-tree) and `npm pack`s (181 files ~400 KB, zero closed paths). **MCP ships in BOTH editions (final user call after two same-day reversals)** — same host code, each instance bound to its own vault; local token secret = per-install ingest secret. **Docker** — `packaging/local/docker/`: Dockerfile builds from the pruned tree; compose = app + `pgvector/pgvector:pg16` (+ optional `--profile gpu` Ollama, Linux/NVIDIA); host Ollama default (macOS can't GPU in containers); `bin/docker-entry.mjs` = container serve (waits PG, schema from `neon/schema.sql` fresh / `db push` upgrade, non-interactive sizing, IMAP poller, real-PG = no pglite shim). **Installers** — `packaging/install.sh` (curl\|sh) + `install.ps1`: Local↔BYOK → Docker↔npm decision tree with flags, per-OS Ollama guidance. Image build not runnable in the agent sandbox (registry egress) — verify in CI/human once | `.dependency-cruiser.cjs`, `scripts/build-local-package.mjs`, `packaging/local/overrides/*`, `packaging/local/docker/{Dockerfile,docker-compose.yml}`, `packaging/local/README.md`, `packaging/install.{sh,ps1}`, `bin/docker-entry.mjs`, CI steps in `.github/workflows/ci.yml`; dep `dependency-cruiser` (dev) |
| **Local edition — first-run sizing + real-Postgres + LOCAL↔BYOK toggle (2026-07-15)**: `datamodo setup` (auto on first `serve`) detects the RAM budget (cgroup-aware) → picks a model tier (4 GB llama3.2:3b · 8 GB llama3.1:8b+llava · 16 GB +llama3.2-vision · 32 GB qwen2.5:14b; always nomic-embed-text) → pulls via Ollama `/api/pull` with progress → seeds `~/.datamodo/llm.json`; fail-soft install hint without Ollama. Settings (local skin): no plan/billing; compute = **Local (this machine) ↔ Bring your own key** toggle (stored `computeMode` unchanged — "cloud" = platform default = host Ollama; plan gate bypassed locally); `LocalAiFields` edits Ollama URL (`llm.json.url`, http(s)-validated, beats `OLLAMA_BASE_URL` env) + text/vision models with live reachability + installed-model datalist (`GET /api/local/llm-models` probes `/api/tags`). `serve` defaults `LLM_PROVIDER=ollama`. **Real Postgres**: `DATABASE_URL` set → normal pooled `@prisma/adapter-pg` (the pglite `max:1`/retry shim now rides `DATAMODO_EMBEDDED_DB=1`, set only when `serve` boots pglite). **`neon/schema.sql` loads into an EMPTY DB** (FK-ordering fix, `\restrict` dropped) — the embedded DB builds fresh vaults from it faithfully (functions/triggers/hnsw; `prisma db push` = upgrade path only) and Docker initdb can mount it. Mock Ollama (`scripts/mock-ollama.mjs`: tags/pull/chat JSON+vision/deterministic 768-d embeddings) runs the pipeline E2E without weights | pure `lib/local/sizing.mjs` (+`tests/local-sizing.test.ts`), runtime `lib/local/setup.mjs`, CLI `setup` + serve hook in `bin/datamodo.mjs`, gate in `lib/prisma.ts`, fresh-load in `lib/local/embedded-db.mjs`, `LocalAiFields` in `control-center.tsx`, probe in `app/api/local/llm-models`, `url` in `lib/local/config.ts`, local guard in `lib/datamodo/settings.ts`, baseUrl thread in `lib/datamodo/llm-for-user.ts`, `neon/schema.sql` |
| **Local edition, phase 1 (2026-07-14)** — npm-installable self-hosted single-user: `datamodo` CLI (`init` scaffolds `~/.datamodo`, `serve` boots the app on localhost, `--version`/`--help`). `DATAMODO_LOCAL=1` → single-user auth (one fixed `LOCAL_USER`, no Neon Auth), `BLOB_DIR` → fs blob storage behind the SAME `putBlob`/`getBlob` chokepoint. LLM is chosen from the dashboard (BYOK/Ollama); embeddings run local (MEMORY). Cloud path untouched — everything is gated on the local flags. **Phase 2 done (2026-07-14): zero-setup embedded database** — `serve` boots pglite (WASM Postgres + pgvector + pg_trgm) behind a local socket (pglite-socket); `lib/prisma.ts` uses `@prisma/adapter-pg` over it when `DATAMODO_LOCAL`; first run builds the schema via `prisma db push` + swaps the embedding indexes to hnsw. No Docker/DB install. **No login (2026-07-15)** — local mode has NO auth UI: `proxy.ts` middleware short-circuits, `/`+login+register+marketing all redirect to `/dashboard`, the `login`/`signup`/`signout` actions no-op into the app, and the header hides sign-out (`ControlCenter local` prop); open localhost → land in the app as the single `LOCAL_USER`. Phase 3 = BYOB connectors + shipping the built Next app in the package; the build-level code-split is still required before OSS ships | CLI `bin/datamodo.mjs` (deps `commander`, `@electric-sql/pglite`+`-socket`, `@prisma/adapter-pg`), pure `lib/local/config.ts`, embedded DB `lib/local/embedded-db.mjs`, adapter switch `lib/prisma.ts`, `lib/storage/blob-fs.ts` + dispatch in `lib/storage/blob.ts`, single-user branch in `lib/auth/session.ts`, no-login in `proxy.ts` + `app/auth/actions.ts` + `app/{login,register,page}.tsx` + `control-center.tsx`, `bin` in `package.json`; tests `tests/local-config.test.ts` + `tests/embedded-db.test.ts` (guarded by `DATAMODO_TEST_DB=1`) |
| Landing page + SEO | `app/page.tsx`, `components/landing/`, `app/landing.css`, `app/robots.ts`, `app/sitemap.ts` |

## Environment variables

**Core (required to run):** `DATABASE_URL` (Neon pooled), `NEON_AUTH_BASE_URL`, `NEON_AUTH_COOKIE_SECRET`.

**LLM (extraction, answers, adjudication):** `LLM_PROVIDER` (`openrouter`|`openai`|`anthropic`|`ollama`, default openrouter) + per provider: `OPENROUTER_API_KEY` / `OPENAI_API_KEY` / `ANTHROPIC_API_KEY` (`ollama` is KEYLESS — `OLLAMA_BASE_URL` default `http://localhost:11434/v1`, optional `OLLAMA_API_KEY` for authed proxies); model overrides `*_EXTRACT_MODEL`, `*_ESCALATE_MODEL`, `*_VISION_MODEL` (vision REQUIRED on OpenRouter — free default can't see; Ollama defaults llama3.1/llava); `OPENROUTER_BASE_URL`/`OPENAI_BASE_URL`/`ANTHROPIC_BASE_URL` (the last also lets E2E drive the real Anthropic provider against mock-ollama's `/v1/messages`), `OPENROUTER_APP_URL`.

**Embeddings (optional, fail-soft):** `EMBEDDINGS_API_KEY` (falls back to `OPENAI_API_KEY`), `EMBEDDINGS_BASE_URL` (a custom URL counts as configured WITHOUT a key — keyless local servers), `EMBEDDINGS_MODEL`, `EMBEDDINGS_DIMENSIONS` (optional OpenAI `dimensions` param) — ⚠️ the model defines the deployment's ONE embedding space (never BYOK); it must emit vectors matching the column type; changing it requires `POST /api/jobs/embed-requeue` until `remaining` is 0. **Local edition (2026-07-15):** `serve` defaults these to a local Ollama server (`http://localhost:11434/v1`, `nomic-embed-text`, 768-dim) when no cloud key/URL is set, and `EMBEDDINGS_COLUMN_DIM` sizes the embedded DB's pgvector columns (default 1536; 768 for the Ollama default) so a local model isn't rejected at store time — all fail-soft to keyword search.

**Transcription (optional, fail-soft — the audio tier is dormant without it):** `TRANSCRIPTION_API_KEY` (falls back to `OPENAI_API_KEY`), `TRANSCRIPTION_BASE_URL` (default `https://api.openai.com/v1`; a custom URL counts as configured without a key), `TRANSCRIPTION_MODEL` (default `whisper-1`).

**Blobs (R2/S3):** `AWS_ENDPOINT_URL_S3`, `AWS_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `BLOB_BUCKET`.

**Ingest & jobs:** `INGEST_WEBHOOK_SECRET` (worker↔app), `INBOUND_EMAIL_DOMAIN`, `CRON_SECRET` (tick + requeue; must match the GitHub Actions secret, with `APP_URL`), `EXTRACT_BATCH`.

**Channels:** `TWILIO_AUTH_TOKEN`, `TWILIO_ACCOUNT_SID`, `TWILIO_WHATSAPP_WEBHOOK_URL`, `TWILIO_WHATSAPP_FROM` (the shared bot number — outbound review pings are dormant without it); `SLACK_SIGNING_SECRET`, `SLACK_BOT_TOKEN` (also powers outbound review pings + reply confirmations); `RESEND_API_KEY` + `EMAIL_FROM` (outbound email review pings — dormant without them; optional `RESEND_BASE_URL` override); `MICROSOFT_APP_ID` (+ dev-only `TEAMS_DEV_SKIP_AUTH`); `APP_URL` (review-ping deep links, falls back to `NEXT_PUBLIC_SITE_URL`).

**Billing (dormant):** `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_PRO`, `STRIPE_PRICE_MAX`.

**MCP (optional):** `MCP_TOKEN_SECRET` (falls back to `NEON_AUTH_COOKIE_SECRET` — always set in real deployments; rotating it revokes every issued token).

**Web:** `NEXT_PUBLIC_SITE_URL` (sitemap/OG).

## Verification bar (what "shipped" means here)
Unit tests on every pure core (`npm test`), `tsc` clean, `next build` green,
lint == baseline, SSR smoke or screenshot for UI, DDL applied + verified on all
Neon branches. Anything NOT verified live (no LLM key / no browser login in the
sandbox) is flagged in PROJECT_STATE's Recent changes.
