# ---------------------------------------------------------------------------
# Template of the environment variables this project expects.
# Copy to .env.local and fill in real values. NEVER commit .env.local.
# ---------------------------------------------------------------------------

# --- App ---
# Base URL used for auth email/OAuth redirects. Localhost in dev, real domain in prod.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Shared secret for the /api/ingest webhook. Provider adapters send it in the
# x-ingest-secret header. Generate with: openssl rand -hex 32
INGEST_WEBHOOK_SECRET=

# --- Supabase client (browser + Next.js SSR via @supabase/ssr) ---
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# --- Supabase server / backend handlers (@supabase/server) ---
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
# Secret key — keep private, server-side only. Never expose to the browser.
SUPABASE_SECRET_KEY=sb_secret_xxx
SUPABASE_JWKS_URL=https://<project-ref>.supabase.co/auth/v1/.well-known/jwks.json

# --- Prisma (Postgres via Supabase poolers) ---
# Transaction-mode pooler (IPv4-only) — used by the app at runtime.
DATABASE_URL="postgresql://postgres.<project-ref>:[YOUR-PASSWORD]@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true"
# Session-mode pooler — used for migrations.
DIRECT_URL="postgresql://postgres.<project-ref>:[YOUR-PASSWORD]@aws-0-<region>.pooler.supabase.com:5432/postgres"

# --- Messaging channels (optional; each channel is inert until set) ---
# The forward-to-a-contact bots. Each platform is ONE shared bot, not one per
# user — users attach via the in-app link code (see app/api/webhooks/README.md).

# WhatsApp Cloud API (Meta). Receiving needs APP_SECRET + VERIFY_TOKEN;
# replying + media download needs PHONE_NUMBER_ID + ACCESS_TOKEN.
# WHATSAPP_BOT_NUMBER="+1 555 010 1234"   # shown to users as the contact to save
# WHATSAPP_APP_SECRET=
# WHATSAPP_VERIFY_TOKEN=                   # you choose this; echoed in the GET handshake
# WHATSAPP_PHONE_NUMBER_ID=
# WHATSAPP_ACCESS_TOKEN=

# Slack app (Events API). Receiving needs the signing secret; replying needs a bot token.
# SLACK_BOT_HANDLE="@datamodo"
# SLACK_SIGNING_SECRET=
# SLACK_BOT_TOKEN=xoxb-...

# Microsoft Teams (Azure Bot Service). App id authenticates inbound; app password mints reply tokens.
# TEAMS_BOT_HANDLE="Datamodo"
# TEAMS_APP_ID=
# TEAMS_APP_PASSWORD=

# --- Billing (optional; upgrades are inert until these are set) ---
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx
# STRIPE_PRICE_PRO=price_xxx
# STRIPE_PRICE_MAX=price_xxx
